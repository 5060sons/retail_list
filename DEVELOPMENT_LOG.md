# 개발 기록 — INK 거래 관리

마지막 업데이트: 2026-09-14

이 문서는 지금까지 진행된 작업 전체를 정리한 기록입니다. 코드 사용법은 [README.md](./README.md)를 참고하세요.

## 1. 프로젝트 개요

거래처와의 매출(공급)·매입(수급) 거래를 기록하고, 월별·업체별로 마감하여 거래명세서를 발행하며, 이를 이메일로 전달하는 내부 업무용 웹사이트. 2~5명(직원 포함)이 함께 사용. 원래 "매출(공급) 거래만" 관리하는 것으로 시작했으나, 이후 "매입(수급) 거래"까지 같은 시스템에서 함께 관리하도록 확장됨.

- 앱 이름: **INK 거래 관리**
- GitHub: https://github.com/5060sons/retail_list
- 배포: Vercel (GitHub 연동, main 브랜치에 푸시하면 자동 재배포)
- DB: Supabase 프로젝트 `Retail_list` (organization `5060sons`, region Seoul `ap-northeast-2`, URL `https://wvdsnmjzmchbioslefrk.supabase.co`)

## 2. 기술 스택

| 항목 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js 16 (App Router) + TypeScript | 서버 컴포넌트/서버 액션으로 별도 백엔드 없이 CRUD 구현 |
| 스타일 | Tailwind CSS | 빠른 UI 작업, 모바일 반응형 대응 |
| DB/인증 | Supabase (Postgres + Auth) | RLS로 권한 제어를 DB 레벨에서 강제 |
| PDF 생성 | @react-pdf/renderer | 서버에서 직접 PDF 스트림 생성, 한글 폰트(Noto Sans KR) 내장 |
| 이메일 발송 | Resend | 도메인 `caretip.co.kr` 인증 완료, 발신 주소 `noreply@caretip.co.kr` |
| 배포 | Vercel | Next.js와 궁합이 가장 좋고 GitHub 푸시 시 자동 배포 |

## 3. 데이터베이스 구조 (`supabase/schema.sql`)

### 테이블
- `profiles` — 로그인 사용자 (역할: `admin` / `staff`)
- `company_settings` — 우리 회사(공급자) 정보, 단일 행
- `customers` — 거래처 마스터. **`partner_type`**(`customer`=공급거래처/매출 대상, `supplier`=수급거래처/매입 대상)으로 구분
- `items` — 품목 마스터
- `transactions` / `transaction_lines` — 거래 헤더/라인. 판매·구매/샘플 구분(`line_type`)은 라인 단위. 공급가·부가세는 `generated always as` 컬럼으로 자동 계산. **거래의 매출/매입 방향은 별도 컬럼 없이 `customers.partner_type`으로 판별** (거래처가 공급거래처면 매출, 수급거래처면 매입)
- `payments` — 수금(매출 거래처) 또는 지급(매입 거래처) 내역 — 방향 무관하게 같은 테이블 재사용
- `monthly_closings` / `customer_closings` — 마감 이력

### 뷰
- `customer_ledger` — 거래처별 거래/수금(지급)을 시간순으로 합쳐 누적 잔액을 윈도우 함수로 계산
- `customer_balances` — 거래처별 "현재" 잔액. **직접 SUM으로 계산** (초기엔 `customer_ledger`의 마지막 행을 집는 방식이었으나, 같은 날짜에 거래와 수금이 겹치면 잘못된 값을 고를 수 있는 버그가 있어 재작성함)

### 보안 (RLS)
- 모든 테이블에 RLS 적용, 로그인 사용자는 조회/입력 가능
- 마감된 거래 수정·삭제, 마감 실행/취소, 거래처·품목 삭제, 사용자 관리, 회사 정보 수정: **admin만 가능**
- `created_by`/`closed_by` 외래키는 `on delete set null` — 사용자 계정을 삭제해도 그 사람이 남긴 거래·거래처·수금 기록은 보존됨 (등록자 정보만 비워짐)

## 4. 인증 / 사용자 관리

- **자율 가입 없음.** 로그인 화면에 "계정 만들기"가 없고, 계정은 **관리자가 `/settings`에서 직접 생성**한다 (이름/이메일/권한 입력 → 임시 비밀번호 자동 발급, 화면에 한 번 표시).
- 최초 관리자 계정은 Supabase 대시보드 Authentication에서 직접 만들면(첫 번째 profiles 행이라) 자동으로 admin 권한을 받는다.
- 로그인한 사용자는 "내 정보"에서 이름과 비밀번호를 스스로 변경 가능.
- 관리자는 사용자 목록에서 권한 변경, 계정 삭제 가능 (본인 계정은 삭제 불가하도록 막음).
- 계정 생성/삭제는 Supabase **service role key**(`SUPABASE_SERVICE_ROLE_KEY`)를 쓰는 관리자 전용 클라이언트(`src/lib/supabase/admin.ts`)로 처리 — 일반 요청 경로와 분리되어 있고 서버에서만 사용.

## 5. 매출(공급)·매입(수급) 통합 관리

거래처 유형(`partner_type`)에 따라 화면 전체가 자동으로 매출/매입 모드로 갈라지는 **통합형** 구조로 구현 (매출/매입용 화면을 따로 만들지 않음).

- **거래처 등록**: 공급거래처(매출) / 수급거래처(매입) 중 하나 선택. 한 거래처는 둘 중 하나만 될 수 있음.
- **거래 등록**: "매출/매입" 토글로 거래처 드롭다운이 해당 유형만 필터링됨. 품목 라인마다 "판매(또는 구매)/샘플" 구분 가능.
- **수금/지급**: 같은 화면·같은 테이블을 쓰되, 거래처 유형에 따라 "수금"/"지급"으로 라벨만 자동 전환.
- **대시보드**: 매출 통계(공급가/부가세/수금액/미수금)와 매입 통계(매입액/매입부가세/지급액/미지급금)를 나란히 표시, 미수금·미지급금 거래처 목록도 분리.
- **거래명세서 PDF**: 매입 거래처 대상이면 공급자/공급받는자 박스가 자동으로 뒤바뀜(거래처가 공급자, 우리 회사가 공급받는자로) — 실제 세금계산서 관례에 맞춤.

## 6. 구현된 기능 요약

- 거래처 관리(`/customers`) — 유형별 필터, CRUD, 상세 페이지에서 거래·수금(지급) 이력과 누적 잔액
- 품목 관리(`/items`)
- 거래 등록/목록(`/transactions`) — 엑셀 스타일 입력 표, 매출/매입 배지·필터, 합계 행
- 수금/지급 관리(`/payments`)
- 대시보드(`/`) — 매출/매입 이번달 통계, 미수금/미지급금 상위 목록
- 월별 마감(`/closing/monthly`) / 업체별 마감(`/closing/customer`) — 마감 후 데이터 잠금, 거래명세서 PDF 출력
- 거래명세서 PDF — 한글 폰트 내장, 방향에 따른 공급자/공급받는자 자동 스왑
- **이메일 발송** — 업체별 마감 화면에서 거래명세서 PDF를 이메일 첨부로 바로 발송 (Resend, `caretip.co.kr` 도메인 인증 완료)
- 설정(`/settings`) — 회사 정보, 사용자 계정 생성/권한변경/삭제, 본인 이름·비밀번호 변경
- 모바일 반응형 — 표 가로 스크롤, 폼 1열 전환, 햄버거 메뉴
- 미설정 안내(`/setup`) — Supabase 환경변수 없으면 자동 이동

## 7. 주요 버그 수정 이력

1. **거래명세서 PDF 500 에러** — `Content-Disposition` 헤더에 거래처 한글 이름을 그대로 넣어서 발생(HTTP 헤더는 ASCII만 허용). RFC 5987 인코딩(`filename*=UTF-8''...`)으로 수정 (`src/lib/pdf/content-disposition.ts`). 초기 빌드 때부터 있던 잠재 버그였음.
2. **거래잔액 계산 오류 가능성** — `customer_balances` 뷰가 "원장의 마지막 행"을 골라 잔액을 표시했는데, 거래와 수금이 같은 날짜에 발생하면(특히 같은 DB 트랜잭션 내 `now()` 동일 시각) 순서를 잘못 판단할 수 있었음. 직접 SUM하는 방식으로 재작성해서 원천적으로 해결.
3. **계정 삭제 시 FK 제약 오류** — `created_by`/`closed_by` 외래키가 기본 `NO ACTION`이라, 거래를 등록한 적 있는 계정은 삭제가 실패했을 것. `ON DELETE SET NULL`로 전체 마이그레이션.

## 8. 개발 중 주요 의사결정

1. **판매/샘플 구분은 거래 헤더가 아닌 품목 라인 단위** — 한 거래 안에 판매·구매 품목과 샘플 품목이 섞일 수 있어서.
2. **매출/매입은 별도 컬럼 대신 거래처 유형으로 판별** — `transactions`/`payments` 테이블 구조를 그대로 재사용할 수 있어 스키마 변경을 최소화. 금액 계산 로직(원장 누적)이 방향에 상관없이 대칭적이라는 점을 활용.
3. **화면은 매출/매입 통합형** — 별도 메뉴로 분리하지 않고 기존 거래처/거래/수금 화면 안에서 유형 필터·라벨 전환으로 처리(사용자 선택).
4. **계정은 관리자가 직접 생성** — 앱이 외부에 공개 배포되면서, 가입 페이지 URL만 알면 누구나 직원 계정을 만들 수 있는 보안 위험을 없애기 위해 자율 가입을 막고 관리자 전용 생성으로 전환.
5. **카카오톡 전달은 "공유하기" 버튼 방식으로 결정** (아직 미구현) — 비즈니스 알림톡 API는 사업자 인증·비용이 필요해서, 무료 공유하기 버튼 방식으로 진행 예정.

## 9. 아직 안 된 것 (다음 단계)

- [ ] 카카오톡 공유하기 버튼 (Kakao Developers 연동 필요 — 로그인 없이 특정 거래명세서 1건만 열람 가능한 임시 공유 링크도 함께 필요)
- [ ] 배포 후 Supabase Auth Redirect URL을 실제 Vercel 도메인으로 업데이트했는지 재확인
- [ ] (선택) 엑셀 일괄 업로드/다운로드
- [ ] (선택) 거래처 여신한도 초과 경고

## 10. 로컬 개발 환경 재현 방법

`README.md`의 "처음 설정하기" 절 참고. 요약하면:
1. `npm install`
2. `.env.local.example` → `.env.local` 복사 후 Supabase 값 채우기 (service role key 포함 — 사용자 계정 생성 기능에 필요)
3. `supabase/schema.sql`을 Supabase SQL Editor에서 실행 (신규 프로젝트인 경우)
4. Supabase 대시보드에서 첫 관리자 계정 직접 생성 (Authentication → Users → Add user)
5. `npm run dev`
